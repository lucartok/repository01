Docker-файл здесь же.

Запустил build командой:
>docker build -t helloapp:v1 .

Затем запустил контейнер командой:
>docker run -d -p 8080:8080 helloapp:v1

А вот так почему-то не работает:
>docker run -d -P helloapp:v1

Вернее работает, но назначает не порт 8080, а, например, 49154